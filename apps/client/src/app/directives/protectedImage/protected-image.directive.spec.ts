import { ProtectedImageDirective } from './protected-image.directive';

describe('ProtectedImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ProtectedImageDirective();
    expect(directive).toBeTruthy();
  });
});
